// src/components/pages/WorkPage.tsx
import { FC } from 'react';
import Work from '@components/organisms/Work';

/**
 * The work page displaying the Work organism.
 * @returns A styled work page.
 */
const WorkPage: FC = () => {
  return <Work />;
};

export default WorkPage;